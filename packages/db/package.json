{
  "name": "@line-crm/db",
  "version": "0.2.0",
  "description": "D1 database query helpers for LINE CRM",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src",
    "bootstrap-meta.json",
    "bootstrap.sql",
    "schema.sql",
    "migrations"
  ],
  "scripts": {
    "generate:bootstrap": "node ./scripts/generate-bootstrap.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.9.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "keywords": [
    "line",
    "crm",
    "d1",
    "cloudflare"
  ],
  "dependencies": {
    "@line-crm/shared": "workspace:*"
  }
}
